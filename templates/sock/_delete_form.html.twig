<form method="post" action="{{ path('sock_delete', {'id': sock.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sock.id) }}">
    <button class="btn">Delete</button>
</form>
