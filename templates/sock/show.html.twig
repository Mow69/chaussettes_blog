{% extends 'base.html.twig' %}

{% block title %}Sock{% endblock %}

{% block body %}
    <h1>Sock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sock.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sock.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ sock.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('sock_index') }}">back to list</a>

    <a href="{{ path('sock_edit', {'id': sock.id}) }}">edit</a>

    {{ include('sock/_delete_form.html.twig') }}
{% endblock %}
