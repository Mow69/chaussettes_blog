{% extends 'base.html.twig' %}

{% block title %}Tie{% endblock %}

{% block body %}
    <h1>Tie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tie.name }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ tie.color }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ tie.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tie_index') }}">back to list</a>

    <a href="{{ path('tie_edit', {'id': tie.id}) }}">edit</a>

    {{ include('tie/_delete_form.html.twig') }}
{% endblock %}
