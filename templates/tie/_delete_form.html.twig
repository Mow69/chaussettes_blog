<form method="post" action="{{ path('tie_delete', {'id': tie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tie.id) }}">
    <button class="btn">Delete</button>
</form>
